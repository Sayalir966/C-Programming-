#include <stdio.h>
int main(){
    int num;
    printf("enter num");
    scanf("%d",&num);
    if (num%2==0)
    {
        printf("%dnum is even\n",num);
        /* code */
    }else
    printf("%dnum is odd\n",num);
    
}